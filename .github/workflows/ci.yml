name: Week26 CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: 🌿 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # 这里我们先假装有一个 requirements.txt，稍后我们会创建它
          echo "Simulating dependency install..."

      - name: 🏃‍♂️ Run Tests
        run: |
          echo "Test 1: Checking code style..."
          echo "Test 2: Running unit tests..."
          echo "🎉 All tests passed!"